# Title

Title

## Usage

``` r
optimize_matrix(sce, sensitivity_threshold = c(0.01, 0.001))
```

## Arguments

- sce:

  sce file

- sensitivity_threshold:

  vector of sensitivity thresholds for rows (variants) and columns
  (cells)
