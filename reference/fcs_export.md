# Title

Title

## Usage

``` r
fcs_export(sce, slot = "DSB_norm", save_path = "~/sample_test.fcs")
```

## Arguments

- sce:

  input sce object

- slot:

  which protein dataset to use. Options are DSB_norm (default), CLR_norm
  or raw

- save_path:

  path to where fcs file should be saved
