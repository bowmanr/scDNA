# Title

Title

## Usage

``` r
compute_clone_statistics(sce, clone_size_cutoff = 10)
```

## Arguments

- sce:

  input sce object

- clone_size_cutoff:

  lower 95% confidence interval cutoff for clone inclusion default=10
