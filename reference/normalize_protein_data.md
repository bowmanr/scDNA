# Normalize Protein Data

Normalize Protein Data

## Usage

``` r
normalize_protein_data(
  sce,
  metadata,
  method = "dsb",
  detect_IgG = TRUE,
  num_components_to_keep = NULL,
  background_droplets
)
```

## Arguments

- NGT:
